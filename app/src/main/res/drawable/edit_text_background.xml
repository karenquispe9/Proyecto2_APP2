<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo gris claro -->
    <solid android:color="#F0F0F0" /> <!-- Gris muy claro -->

    <!-- Borde suave (opcional) -->
    <stroke
        android:width="1dp"
        android:color="#D0D0D0" /> <!-- Gris más oscuro para el borde -->

    <!-- Bordes redondeados -->
    <corners android:radius="8dp" />

    <!-- Espaciado interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>